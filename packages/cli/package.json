{
  "name": "@atproto-agent/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "zap": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "build:binary": "bun build ./src/index.ts --compile --outfile dist/zap",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atproto-agent/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
