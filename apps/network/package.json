{
  "name": "@atproto-agent/network",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atproto-agent/core": "workspace:*",
    "@atproto-agent/agent": "workspace:*"
  }
}
